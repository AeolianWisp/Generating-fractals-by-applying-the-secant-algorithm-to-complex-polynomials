# Generating-fractals-by-applying-the-secant-algorithm-to-complex-polynomials
A program which colours points in the complex plane based on which root of a polynomial the secant algorithm takes it to (if any). This creates unique fractals for each polynomial.
